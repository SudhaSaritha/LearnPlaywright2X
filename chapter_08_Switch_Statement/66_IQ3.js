let x = 10;
// it will allow you to have a duplicate case with first as a usage

switch (x) {
    case 10:
        let b1 = 1;
        console.log("the b1 value is:" + b1);
        break;

    case 10:
        let b2 = 2;
        console.log("the b2 value is: " + b2);
        break;

    default:
        console.log("d");
}