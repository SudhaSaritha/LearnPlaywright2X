let statusCode = 200;

if (statusCode === 200) {
    console.log("API are working fine!");
} else if (statusCode === 400) {
    console.log("API not found!");
} else {
    console.log("no status code is matched!");
}